#masonry-container.transitions-enabled.infinite-scroll
	-@photos.each do |ph|
		= link_to photo_path(ph) do
			.box
				%h3= ph.title
				=image_tag(ph.image.url, alt: "#{ph.title}")
				%h4= ph.city.name
